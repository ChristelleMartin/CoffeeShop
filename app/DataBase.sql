-- Generated by Mocodo 4.2.8

CREATE TABLE Inclure (
  PRIMARY KEY (id_recipe, id_ingredient),
  id_recipe     SMALLINT NOT NULL,
  id_ingredient     SMALLINT NOT NULL,
  quantity SMALLINT
);

CREATE TABLE Ingredient (
  PRIMARY KEY (id),
  id   SMALLINT NOT NULL,
  name VARCHAR(255) NOT NULL
);

CREATE TABLE Recipe (
  PRIMARY KEY (id),
  id            SMALLINT NOT NULL,
  title         VARCHAR(255) NOT NULL,
  instruction   VARCHAR(255),
  Ingredient_id SMALLINT,
  Type_id       SMALLINT,
  Created_at DATE
);

CREATE TABLE Type (
  PRIMARY KEY (id),
  id   SMALLINT NOT NULL,
  name VARCHAR(255) NOT NULL,
  id_ingredient SMALLINT NOT NULL
);

ALTER TABLE Inclure ADD FOREIGN KEY (id_ingredient) REFERENCES Ingredient (id);
ALTER TABLE Inclure ADD FOREIGN KEY (id_recipe) REFERENCES Recipe (id);

ALTER TABLE Type ADD FOREIGN KEY (id_recipe) REFERENCES Recipe (id);


Autre solution :

CREATE TABLE types (
    id INT PRIMARY KEY AUTO_INCREMENT,
    type_name VARCHAR(50) NOT NULL
);

CREATE TABLE ingredients (
    id INT PRIMARY KEY AUTO_INCREMENT,
    ingredient_name VARCHAR(100) NOT NULL
);

CREATE TABLE recipes (
    id INT PRIMARY KEY AUTO_INCREMENT,
    recipe_name VARCHAR(100) NOT NULL,
    type_id INT,
    instructions VARCHAR(255),
    Created_at DATE,
    FOREIGN KEY (type_id) REFERENCES types(id)
);

CREATE TABLE recipe_ingredients (
    recipe_id INT,
    ingredient_id INT,
    quantity VARCHAR(50),
    PRIMARY KEY (recipe_id, ingredient_id),
    FOREIGN KEY (recipe_id) REFERENCES recipes(id),
    FOREIGN KEY (ingredient_id) REFERENCES ingredients(id)
);